import { heroui } from '@heroui/react';

export default heroui({
	themes: {
		light: {
			colors: {
				background: 'var(--color-background)',
			},
		},
		dark: { colors: { background: 'var(--color-background)' } },
	},
});
