/**
 * Funções de cookie usando a API nativa do Next.js
 * Server Actions para manipulação de cookies
 */

export { getCookie, removeCookie, setCookie } from './actions';
export { getClientCookie } from './client';
